{
  "name": "mon-site-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "git add -A && git commit -m \"Mise à jour rapide\" && git push",
    "compress": "node compress-images.js",
    "blur": "node blur-secret-images.js"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.91.1"
  }
}
